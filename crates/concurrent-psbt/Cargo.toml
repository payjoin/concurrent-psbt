[package]
name = "concurrent-psbt"
version.workspace = true
edition = "2024"

[features]
default = ["unit-tests", "prop-tests"]
unit-tests = []
prop-tests = []

[dev-dependencies]
proptest = "1"

[lints]
workspace = true
